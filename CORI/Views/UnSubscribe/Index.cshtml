
@{
    ViewData["Title"] = "Unsubscribe";
    Layout = "~/Views/Shared/_Survey-Layout.cshtml";
}

<script>

    function myfunction() {
        $.post("@Url.Action("Unsubscribe", "Unsubscribe")", { email: "@Model" }, function (result) {
            if (result.error) {
                // Something bad happened
                // Show an error
            }
            else {
                // Show the all clear message
            }
        });
    }

</script>

<h2>Unsubscribe From Driven</h2>

<a asp-action="Survey" asp-controller="Survey" target="_blank"><input type="button" class="btn btn-default" style="display:block;" value="Unsubscribe"></a>